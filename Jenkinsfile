pipeline {
    agent any
    stages {
        stage('Initialazation') {
            steps {
              sh 'echo inside intializa stage QA branch'
            }
        }
        stage('Bye') {
            steps {
                sh 'echo Bye QA Branch'
            }
        }
    }
}
