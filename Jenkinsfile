pipeline {
    agent any
    stages {
        stage('Initialazation') {
            steps {
              sh 'echo inside intializa stage dev branch'
            }
        }
        stage('Bye') {
            steps {
                sh 'echo Bye dev Branch'
            }
        }
    }
}
